{% extends 'base.html.twig' %}

{% block title %}Liste des contacts - AddressBook{% endblock %}

{% block body %}
    <h2>Liste des contacts</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Nom complet</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>Jean Dupont</td>
                <td>
                    <a class="btn btn-sm btn-primary" href="{{ path('app_contact_show', {id: 123}) }}">Afficher</a>
                    <a class="btn btn-sm btn-warning" href="{{ path('app_contact_update', {id: 123}) }}">Modifier</a>
                    <a class="btn btn-sm btn-danger" href="{{ path('app_contact_delete', {id: 123}) }}">Supprimer</a>
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
